<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
</head>

<body>
	<h1>FRUIT: <%= fruitDoc.name %>
	</h1>

	<% if (fruitDoc.isReadyToEat === true) { %>
		<span>Ready to eat</span>
	<% } else { %>
		<span>Not Ready to eat</span>
	<% } %>

	<form method="POST" action="/fruits/<%= fruitDoc._id %>?_method=DELETE">
		<button type="submit">Delete resource</button>
	</form>
	<a href="/fruits/<%= fruitDoc._id %>/edit">Edit <%= fruitDoc.name %></a>

</body>

</html>